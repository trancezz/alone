# Spring Datasource Druid JDBC Configuration
spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/alone?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
      username: root
      password: zz1994
      driver-class-name: com.mysql.cj.jdbc.Driver
      # Spring Datasource Druid Connection Pool Configuration
      initial-size: 10
      max-active: 100
      min-idle: 10
      max-wait: 6000
      pool-prepared-statements: false
      max-pool-prepared-statement-per-connection-size: -1
      validation-query: select 'x'
      validation-query-timeout: 1
      test-on-borrow: false
      test-on-return: false
      test-while-idle: true
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 60000
      # Option Druid Plugins
      filter:
        wall:
          enabled: true
          db-type: mysql
          config:
            metadata-allow: true
            wrap-allow: false
            truncate-allow: false
          log-violation: true
          throw-exception: false
        stat:
          enabled: true
          db-type: mysql
          merge-sql: true
          slow-sql-millis: 2000
          log-slow-sql: true
        log4j:
          data-source-log-enabled: true
  redis:
    host: 127.0.0.1
    port: 6379
    timeout: 6000
  application:
    name: alone

server:
  port: 8801